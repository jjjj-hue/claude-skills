---
name: pull-media
description: >
  Alias for /email-blast. Use email-blast instead — it pulls media (Jessie's YouTube first,
  then Matterport, then SE photos) AND formats the HTML blast in one step.
---

Use `/email-blast` — it handles media pulling and HTML formatting in one flow.

Media priority built into email-blast:
1. Jessie's YouTube (`@jessieoriolhuaman`) — search by address/unit first
2. Matterport — check StreetEasy building page
3. StreetEasy photos — up to 6, extracted directly from listing page
